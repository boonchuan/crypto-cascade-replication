# Direct dependencies, pinned to the versions used for all published results.
# Python 3.14.3 (MSC v.1944 64-bit, Windows).
# environment-full.txt is the complete pip freeze of the authoring environment.

matplotlib==3.10.9
numpy==2.5.1
pandas==2.3.3
pyarrow==23.0.1
requests==2.34.2
scipy==1.17.1
statsmodels==0.14.6


